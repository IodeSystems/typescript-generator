#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/.."
./gradlew releaseStripSnapshotCommitAndTag && \
  ((./gradlew publishToMavenLocal && ./gradlew releasePublish && ./gradlew releasePrepareNextDevelopmentIteration) || ./gradlew releaseRevert)
